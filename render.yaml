services:
  - type: web
    name: BCA-1
    env: python
    rootDir: BCA
    buildCommand: "pip install pipenv && pipenv install"
    startCommand: "pipenv run python run.py"
    region: singapore
    plan: free
